# Slim requirements: pandas/pyarrow/numpy/Faker come from the data_libs layer.
# This Lambda only needs the Snowflake connector inline.
#
# Total unzipped size with this slim install: ~30 MB (snowflake-connector +
# its transitive deps cryptography, pyOpenSSL, etc.). Well under the 250 MB
# function cap; pandas+pyarrow live in the layer's separate 250 MB allowance.

snowflake-connector-python==3.13.2
